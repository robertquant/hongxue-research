server {
    listen 80;
    server_name your-domain.com;

    root /var/www/hongxue-research/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000/;
    }
}
